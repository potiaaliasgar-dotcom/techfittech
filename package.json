{
  "name": "techfittech-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "optimize-images": "node scripts/optimize-images.mjs",
    "build": "npm run optimize-images && node scripts/generate-pdf-guide.js && rm -rf dist && vite build && node scripts/generate-seo-pages.mjs && node scripts/validate-schemas.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.6",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.19",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
