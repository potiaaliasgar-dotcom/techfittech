{
  "name": "techfittech-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "optimize-images": "node scripts/optimize-images.mjs",
    "build": "rm -rf dist\\ * dist_* 'dist 2' 'dist 3' 'dist 4' 'dist 5' 'dist 6' && node --check public/assets/app.js && node scripts/guard-valid-pages.mjs && npm run optimize-images && node scripts/generate-pdf-guide.js && rm -rf dist && vite build && node scripts/generate-seo-pages.mjs && npx terser dist/assets/app.js -o dist/assets/app.js -c -m && node scripts/hash-assets.mjs && node scripts/validate-schemas.mjs && node scripts/guard-sitemap.mjs && node scripts/guard-llms.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.6",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.19",
    "terser": "^5.50.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
