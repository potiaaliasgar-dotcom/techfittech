{
    "buildCommand": "npm run build && node scripts/generate-seo-pages.mjs",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}