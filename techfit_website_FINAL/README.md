# TechFit Website — Upload (FINAL v3.4)

## Contents
- `index.html` — homepage (SPA)
- `robots.txt` + `sitemap.xml`
- `assets/images/` — all images

## Upload
1. Upload everything to web root
2. Keep `assets/` structure intact
3. Visit `https://www.techfittech.com/`

## Ali's side tasks
- Upload `favicon.ico`, `apple-touch-icon.png`, `og-image.jpg` to web root
- Click FormSubmit activation link in `techfitpa@gmail.com` after first test submission
- Add GA4 if desired
